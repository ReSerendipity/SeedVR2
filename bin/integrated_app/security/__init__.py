"""SeedVR2 - 安全模块包。

集中管理应用安全相关组件，提供纵深防御体系：

核心安全模块:
    - path_guard: 路径白名单守卫，防止路径遍历攻击和任意文件访问

安全设计原则:
    - 白名单优于黑名单：默认拒绝所有访问，仅显式配置的路径允许访问
    - 默认安全 (Secure by Default)：未配置时不允许任何路径访问
    - 最小权限原则：仅授予应用完成功能所需的最小文件系统访问权限
    - 纵深防御：路径解析后再次校验，防御符号链接、相对路径等绕过手段

安全边界:
    - 所有文件系统访问（扫描、下载、上传）必须经过 PathGuard 校验
    - 禁止直接将用户输入拼接为文件路径而不经过安全检查
    - 安全模块不应依赖业务逻辑，保持独立可测试
"""
